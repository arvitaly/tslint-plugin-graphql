const f = {
    a: (literals: any, ...placeholders: any[]) => {
        // 
    },
};
let b = f.a`{
            allFilms {
              films {
                title,
                director
              }
            }
}`;
